## Plan: Add 10 Interactive Demos to API Pages

Following the exact pattern from commit 4888f77, I'll add interactive demos to 10 more API pages across `sun` and `scenery-phet`.

### Files to Create (10 demo modules)

All in `docs/.vitepress/demos/`:

1. **`v-slider.ts`** — VSlider with a NumberProperty (0–100), a readout Text, and a VBox layout. Shows the vertical slider interaction.
2. **`toggle-switch.ts`** — ToggleSwitch toggling between 'metric'/'imperial' with a readout showing current value.
3. **`radio-button-group.ts`** — RectangularRadioButtonGroup with 'circle'/'square'/'triangle' options and a readout.
4. **`panel.ts`** — Panel wrapping a VBox of Text and Checkbox, showing the bordered container.
5. **`accordion-box.ts`** — AccordionBox with title "Advanced" and expandable content (Text nodes), starting collapsed.
6. **`text-push-button.ts`** — TextPushButton that increments a counter on each press with a readout.
7. **`number-spinner.ts`** — NumberSpinner with integer steps 0–10 and a readout showing current value.
8. **`thermometer-node.ts`** — ThermometerNode bound to a NumberProperty with a slider (HSlider) to adjust temperature.
9. **`face-node.ts`** — FaceNode with a RectangularPushButton that toggles between smile/frown.
10. **`arrow-node.ts`** — ArrowNode that changes direction/length on button press, or follows the mouse.

### Files to Modify

1. **`docs/.vitepress/demos/types.ts`** — Add 10 new IDs to the `DemoId` union type.
2. **`docs/.vitepress/demos/registry.ts`** — Add 10 new entries to `DEMO_REGISTRY`.
3. **10 API markdown pages** — Insert `<SceneryDemo demo="<id>" />` after the primary code snippet, following the checkbox.md pattern:
   - `docs/api/sun/v-slider.md`
   - `docs/api/sun/toggle-switch.md`
   - `docs/api/sun/radio-button-group.md`
   - `docs/api/sun/panel.md`
   - `docs/api/sun/accordion-box.md`
   - `docs/api/sun/text-push-button.md`
   - `docs/api/sun/number-spinner.md`
   - `docs/api/scenery-phet/thermometer-node.md`
   - `docs/api/scenery-phet/face-node.md`
   - `docs/api/scenery-phet/arrow-node.md`

### Demo Design Details

Each demo follows the established pattern:
- `export const width` and `export const height` for fixed canvas dimensions
- `export function createDemo( rootNode: Node ): () => void` returning a dispose function
- Uses `centerInDisplay` for centering
- Uses `Tandem.OPTIONAL` (not REQUIRED)
- Full cleanup in the dispose function

### Verification

- Run `npm run demos:check` to validate all registered IDs have corresponding files with `createDemo`
- Run `npm run build` to ensure the VitePress site builds successfully